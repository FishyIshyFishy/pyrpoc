"""pyrpoc, rebuilt (routines refactor).

Built alongside the existing ``pyrpoc`` package; renamed into place at the swap.
Layering, bottom to top: structs -> instruments -> acquisition -> core -> gui.
"""
