**UI visual problems**
1. Instance cards do not correctly resize when multiple are open at the same time
2. Backdrop behind the buttons to click AI0, AI1, ..., as in the thing that contains all of the toggles that goes to the right of "Active AI Channels", is the wrong color. Should be the same color as t 

---------
**UI usability problems** 
1. The display interface is overly complicated from a user perspective. But what is the right approach?
2. The ImageJ-esque tools are ok, but they are clunky as is. What specifics are needed to make them actually good?

---------
**Microscopy-user problems**
1. Sample rate initializes to 100 kHz, should be MHz. The units should be in MHz as well, that is a more standard thing to work with (0.1 increments)

---------
**Necessary future features (DO NOT IMPLEMENT YET)**
1. The whole GUI looks weird because of fonts. What is a better UI? 